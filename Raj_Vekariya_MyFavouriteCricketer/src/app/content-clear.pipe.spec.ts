import { ContentClearPipe } from './content-clear.pipe';

describe('ContentClearPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentClearPipe();
    expect(pipe).toBeTruthy();
  });
});
